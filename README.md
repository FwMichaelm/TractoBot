# TractoBot
We built a 2-wheel drive robot using SnappyXO pieces to represent a tractor moving through a crop field. We then designed a linear actuator marking mechanism in MotionGen, built it with SnappyXO pieces and mounted it onto the robot. We programmed the robot to navigate a 3x3 grid, stopping at each plant to scan it and determine if its healthy. 
